/**
 * Consolidated Firebase web SDK accessors (Auth + Firestore).
 * Prefer importing from `@/lib/firebase` in services/features.
 */
export { getFirebaseApp, getFirebaseAuth, getFirebaseDb } from "@/lib/firebase/client";
