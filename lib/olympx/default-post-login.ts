/**
 * Default app route after successful sign-in or registration when no `next` query is present.
 * Uses dashboard layout + sidebar (`app/(dashboard)`).
 */
export const DEFAULT_POST_LOGIN_PATH = "/organizations/create";
