/** Shared storage + cookie name for Olympx session (no browser APIs — safe for middleware). */
export const OLYMPX_ACCESS_TOKEN_KEY = "olympx_access_token";

export const OLYMPX_AUTH_JSON_KEY = "olympx_auth";

export const OLYMPX_SESSION_CHANGED = "olympx-session-changed";
